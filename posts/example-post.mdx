---
title: Tailwind MDX Issue
description: This frontmatter description will appear below the title
---

<main className="absolute inset-0 overflow-hidden bg-pink-800 text-white">
    <section className="h-full w-full">
        <div className="h-full mx-auto container">
            <div className="h-full w-full flex column justify-center p-40">
                <div className="m-auto text-center">
                    <h1>{title}</h1>  
                    SOME Tailwind classes do not work in mdx if JIT is active in tailwind.config.js. Uncomment/comment mode:jit in tailwind.config.js, run `npm run dev`again to see the behaviour in the webbrowser. If this background is pink, Tailwind works because JIT is deactivated.
                </div>
            </div>
        </div>
    </section>
</main>